import { BrowserRouter as Router, Route } from 'react-router-dom';

function Routes() {
  return (
    <Router forceRefresh>
      <Route path='/sign' exact component={} />
    </Router>
  );
}

export default App;
